# No module-specific runtime dependencies.
#
# This module is pure HTTP and uses only the Python standard library
# (csv, io, json, datetime) plus sweatpants core primitives:
#   - sweatpants.Module               (base class + send_signed_callback, sweatpants#26)
#   - sweatpants.proxied_request      (HTTP through the rotating proxy)
#
# httpx (the transport behind proxied_request) is a core sweatpants dependency
# (see sweatpants/pyproject.toml), so nothing extra is required here.
